typedef double num;
typedef char *string;

const num PI = 3.14;
// Easy loop for the for loop

#define newline printf("\n");
#define loopI(SIZE) for (int i = 0; i < SIZE; i++)
#define print(VALUE)                                                           \
  printf(VALUE);                                                               \
  ;
#define printl(VALUE)                                                          \
  printf(VALUE);                                                               \
  newline;
